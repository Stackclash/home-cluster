ingress:
  enabled: true
  hosts:
    - host: openfaas.$DOMAIN
      serviceName: gateway
      servicePort: 8080
      path: /
  annotations:
    kubernetes.io/ingress.class: "external"
  tls:
    - hosts:
        - openfaas.$DOMAIN
