---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: acme-crt
  namespace: networking
spec:
  secretName: acme-crt-secret
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - "${DOMAIN}"
  - "*.${DOMAIN}"
  - "*.internal.${DOMAIN}"
