---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: grocy
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 1Gi
  nfs:
    path: "${NFS_BASE_PATH}/Apps/grocy"
    server: "${NFS_SERVER_IP}"
  storageClassName: grocy
  volumeMode: Filesystem
  persistentVolumeReclaimPolicy: Retain
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grocy
  namespace: home
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: grocy
  volumeMode: Filesystem
