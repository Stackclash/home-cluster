ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: "external"
  tls:
    - hosts:
        - calibre.$DOMAIN
  hosts:
    - host: calibre.$DOMAIN
      paths:
        - path: /
          pathType: Prefix
