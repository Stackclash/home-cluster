ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: "external"
  tls:
    - hosts:
        - booksonic.$DOMAIN
  hosts:
    - host: booksonic.$DOMAIN
      paths:
        - path: /
          pathType: Prefix
