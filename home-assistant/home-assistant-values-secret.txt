ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: "external"
  tls: 
    - hosts:
        - $DOMAIN
  hosts:
    - $DOMAIN
vscode:
  password: $PASSWORD
  ingress:
    enabled: true
    annotations:
      kubernetes.io/ingress.class: "internal"
    hosts:
      - code.internal.$DOMAIN