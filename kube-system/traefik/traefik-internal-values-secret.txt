ingressRoute:
  dashboard:
    enabled: true
    annotations:
      kubernetes.io/ingress.class: traefik-internal
    labels:
      traefik.http.routers.dashboard.rule: "Host(`traefik.internal.$DOMAIN`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard`))"